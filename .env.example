# Banco de dados (Docker — ver docker-compose.yml)
DATABASE_URL="postgresql://alexia:alexia_dev_pw@localhost:5432/alexia_camara?schema=public"

# NextAuth
# Gere um valor com: openssl rand -base64 32
NEXTAUTH_SECRET="troque-por-um-segredo-aleatorio-antes-de-subir-para-producao"
NEXTAUTH_URL="http://localhost:3000"

# Base usada para montar os links de acesso da família (ex: https://app.alexiacamara.com.br)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
