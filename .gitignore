node_modules
.next
.env
.env.local

# Arquivos enviados pelos usuários (fotos, exames, documentos) — dados
# sensíveis de paciente, nunca devem ir para o controle de versão.
# Em produção, trocar o storage local por um provedor externo (S3 etc.)
uploads/

.vercel

.vercel
